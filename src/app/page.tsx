'use client';

import TradesStream from "../components/TradesStream";

export default function Home() {
  return (
    <div className="h-screen p-4 font-[family-name:var(--font-geist-sans)]">
      <TradesStream />
    </div>
  );
}
